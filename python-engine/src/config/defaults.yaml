# SENTIENT_OS v2 Default Settings

api:
  gemini_api_key: ""

server:
  ws_host: "127.0.0.1"
  ws_port: 0  # 0 = random port

safety:
  kill_switch_enabled: true
  cpu_critical: 90
  ram_critical_mb: 750

horror:
  intensity: "medium"  # mild, medium, extreme
  language: "tr"

tts:
  tts_voice: "tr-TR-AhmetNeural"

paths:
  db_path: "data/sentient.db"
  temp_dir: "temp/"
  log_dir: "logs/"
